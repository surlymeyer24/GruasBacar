# Modo `vite` por defecto (`npm run dev`): siempre emuladores.
# Las claves del proyecto siguen en `.env.local` (solo identifican el projectId).
# El tráfico va a localhost, no a Firestore/Auth/Storage reales.
VITE_USE_EMULATORS=true
