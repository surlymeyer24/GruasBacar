# Copiá este archivo a frontend/.env.live para levantar el dev server
# contra el proyecto Firebase real (sin emuladores), usando `npm run dev:live`.
# .env.live no se commitea (está en .gitignore).

VITE_USE_EMULATORS=false
